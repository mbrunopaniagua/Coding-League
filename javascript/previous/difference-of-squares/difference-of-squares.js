const computeSquareOfSumTo = input  => {
  return 0;
}

const computeSumOfSquaresTo = input  => {
  return 0;
}

const computeDifferenceOfSquares = input => {
  return 0;
}

module.exports = { computeSquareOfSumTo, computeSumOfSquaresTo, computeDifferenceOfSquares };

